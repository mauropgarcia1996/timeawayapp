import React from 'react';
import {ScrollView, View} from 'react-native';
import {Text} from 'react-native-paper';

const Home = () => {
  return (
    <ScrollView>
      <View>
        <Text>Home!</Text>
      </View>
    </ScrollView>
  );
};

export default Home;
